spring:
  application:
    name: employee-delete-service
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/${DB_ENDPOINT}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
endpoint: ${ENDPOINT}
